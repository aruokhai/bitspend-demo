import { ComponentFixture, TestBed } from '@angular/core/testing';

import { BankListInputComponent } from './bank-list-input.component';

describe('BankListInputComponent', () => {
  let component: BankListInputComponent;
  let fixture: ComponentFixture<BankListInputComponent>;

  beforeEach(() => {
    // TestBed.configureTestingModule({
    //   declarations: [BankListInputComponent]
    // });
    // fixture = TestBed.createComponent(BankListInputComponent);
    // component = fixture.componentInstance;
    // fixture.detectChanges();
  });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
